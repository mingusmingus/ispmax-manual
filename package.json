{
  "name": "ispmax-manual",
  "version": "2.0.0",
  "private": true,
  "description": "Manual de usuario de ISPMAX — documentación construida con Rspress 2",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "assets": "node scripts/sync-assets.mjs",
    "dev": "npm run assets && rspress dev",
    "build": "npm run assets && rspress build",
    "preview": "rspress preview",
    "migrate:html": "node scripts/html-to-mdx.mjs"
  },
  "dependencies": {
    "@rspress/core": "^2.0.18"
  },
  "devDependencies": {
    "node-html-parser": "^6.1.13",
    "typescript": "^5.6.3",
    "@types/react": "^19.0.0"
  }
}
